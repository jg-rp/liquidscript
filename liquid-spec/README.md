A JSON-RPC service and build script for running LiquidScript against liquid-spec.

## Usage

To build a standalone executable, `bin/liquidscript_service`, from the root of the repository:

```
bun spec:build
```
